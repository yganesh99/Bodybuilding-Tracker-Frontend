// import { selectWorkoutsPageDomain } from '../selectors';

describe('selectWorkoutsPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
